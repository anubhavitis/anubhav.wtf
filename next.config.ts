import type { NextConfig } from "next";

// Import server initialization (this will run when the config is loaded)
import './src/lib/server-init';

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
